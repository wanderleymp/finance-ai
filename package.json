{
  "name": "finance-ai",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "api",
    "app/*",
    "landing",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean && rm -rf node_modules",
    "test": "jest"
  },
  "devDependencies": {
    "eslint": "^8.45.0",
    "prettier": "^3.0.0",
    "tsup": "^7.2.0",
    "turbo": "^1.10.12",
    "typescript": "^5.1.0"
  }
}
